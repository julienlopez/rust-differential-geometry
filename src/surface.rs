use crate::expression::{Expression, Variable};

pub struct Surface {}

impl Surface {
    pub fn from_embedding(surface_variables: Vec<Variable>, embedding: Vec<Expression>) -> Surface {
        Surface {}
    }
}
